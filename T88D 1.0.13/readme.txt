1.0.13 2016.07.11
1、串口命令更改：删除（1:检测插入：0x24 0x01... ，2:读取 0x24 0x32 ...）
2、增加Flash读写：存储三个口设置的偏置距离的值。
3、串口握手命令中rx1buf[14]的第七八位为左舷状态，五六位为网尾状态 ，三四位为右舷状态，收到握手命令后判断该网位仪为新插入的发送注入命令，该注入命令的第八位置1。
4、设置出厂默认值为:左舷：x：50，y：400 ；网尾：x：0，y：500 ； 右舷：x：50，y:400

1.0.12 2016.06.24
添加按键防抖

1.0.11 2016.06.24
故障处理:出现故障后，再次选择故障的网位仪，会发送读取命令，如果收到说明该网位仪正常。选择正常的网位仪，如果发送三次打开串口命令都没收到回复，判断该网位仪为故障

1.0.10 2016.06.23
创建消息队列，用来串口发送数据
在串口发送任务中请求消息队列，如果有东西就发送

1.0.9 2016.06.23
加入UCOS操作系统

v1.0.8 2016.06.21
1、串口中断改为接收中断，不用DMA接收

v1.0.7 2016.06.20
1、判断为故障的网位仪后，再次选择此网位仪，会发送读取命令，如果收到回复则说明该网位仪正常
2 、10s内发生命令没有收到回复，判断该网标为故障闪灯

v1.0.6 2016.6.17
1、修正定时器1溢出中断，定时器配置中加一句TIM_RepetitionCounter = 0；

v1.0.5 2016.6.16 (目前烧写程序)
当判断为T81断开后，再接收到握手命令回复恢复之前的状态

v1.0.4 2016.6.6
T88到T81检测：T88定时（2秒）给T81发送串口数据，（0x24,5a...）,三次中有收到至少一次，说明正常状态，否则T88不显示任何东西
定时器1 500ms ：判断串口等待超时，超时时间2秒

v1.0.3 2016.6.3
故障判断,判断为故障后亮红灯

v1.0.2 2016.6.3
串口状态判断，当发出命令后，串口处于等待状态，等待状态是不允许进行其他命令的发送，收到
命令回复后，串口状态变为空闲状态

v1.0.1 2016.6.3
按键屏蔽，当选择一个网位仪后，再次选择当前的网尾，不做任何处理

v1.0.0  2016.5.28


